services:
  - type: web
    name: pulpo-automation
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
```

---

**requirements.txt** bleibt gleich:
```
flask
requests
gunicorn
